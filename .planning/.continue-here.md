---
project: SPARK
status: in_progress
last_updated: 2026-03-18T19:50:13.527Z
branch: RubenBranch
---

<current_state>
Features de esta sesion completadas y pusheadas a deploy. Esperando siguiente indicacion del usuario.
</current_state>

<completed_work>
Sesiones anteriores (items 1-104 ya documentados).

Esta sesion:
105. fix: Estatus "Borrador" race condition en deploy - pendingEstatusId pattern para que el valor se aplique solo cuando opciones del Select estan cargadas
106. feat: Boton "Crear Cotizacion" cambia a "Actualizar Cotizacion" despues de crear - handleSubmit usa actualizarCotizacion cuando cotizacionId existe (sin necesidad de editId en URL)
</completed_work>

<remaining_work>
- Usuario indicara siguientes features
</remaining_work>

<decisions_made>
- API route /api/salones en vez de server action para evitar cancelacion por re-render React
- window.history.replaceState en vez de router.replace para persistir URL sin remontar componentes
- Columna salonid (no saloncombinadoid) en tabla salones para combinaciones, formato JSON {"salon1":id,"salon2":id}
- obtenerSalonesRelacionados: busca bidireccional (padre->hijos y hijo->padres)
- isCancelada() usa toLowerCase().includes("cancelada") para comparacion case-insensitive
- Colores dashboard: purple-900/80 reservaciones, amber-400 cotizaciones, red-600/85 canceladas
- listaDesplegableTipoEvento acepta parametro categoriaevento para filtrar
- buscarPlatillosItems acepta platilloid, hotelid, y tipo para filtrar
- Estatus "Borrador" match con trim() porque BD tiene espacio trailing
- Dark mode: sidebar usa spark-icon-dark.svg, header usa spark-green.svg; light: spark-icon.svg, spark-black.svg
- tipopaquete viene de tabla paquetes (no en vw_paquetes), se enriquece en listaDesplegablePaquetes
- platillositems.tipo distingue ENTRADAS, PLATO FUERTE, POSTRES para paquetes Completo
- Race condition fix: usar useEffects con pending states para Select de Radix en vez de .then() encadenados
- Modal paquete: estados preview separados (previewPaqueteId/previewPaqueteInfo) para no afectar formato asignado
- Estatus en creacion: pendingEstatusId pattern (igual que salon/montaje/tipoEvento) para evitar race condition en deploy
- Boton crear/actualizar: usa existingId = editId || cotizacionId para determinar si crear o actualizar
</decisions_made>

<context>
Stack: Next.js 16 + React 19 + Supabase + Shadcn/ui
Ruta: C:\Claude\projects\SPARK
GitHub: https://github.com/adornodavid/SPARK
Dev server: npm run dev en localhost:3000
Deploy: Vercel auto-deploy via git integration (projectId: prj_WrF8PnNFuzpw8OpSa2L9vFtPejhO, teamId: team_DFlhS1OFbGUsPUIlOnGK1MWg)
</context>

<next_action>
Esperando indicacion del usuario para siguientes features.
</next_action>
