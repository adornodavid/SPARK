---
phase: session-2026-04-27
task: shipped
total_tasks: 17
status: shipped
last_updated: 2026-04-28T04:09:15.194Z
---

<current_state>
Sesión completa shippeada en commit `15e72b0` (+885/-632, 11 archivos), pusheada a `origin/main`. Vercel auto-deploy disparado. Working tree limpio, local sincronizado con origin (incluye 2 commits previos de Omar: `e73a96a` /perfil + `8d9b4cc` usuarios.hotelid).

Dev server corriendo en localhost:3000 (Next.js 16.0.10 Turbopack). Último compile exitoso.

Pendiente: validación user en prod (logout/login fresh + recorrer features) y atacar arrastres de sesiones previas (tareas 21-23).
</current_state>

<completed_work>

**17 features shippeadas en `15e72b0`:**

1. `eventos.estatuscomercialid` se inserta en `crearCotizacion` (default 1, parametrizado vía form FormData)
2. Cookie de sesión incluye `HotelId` desde `usuarios.hotelid` (hecho por Omar en 8d9b4cc, mi implementación local descartada por equivalencia)
3. Dashboard preselect del hotel del usuario (de `session.HotelId`); URL no necesita `?hotelId=`
4. Vista Disponibilidad: agregada sidebar Próximos Eventos + grid 3:1 (calendario amplio + sidebar 25%)
5. Sidebar Próximos Eventos compactada (text-[10px], iconos h-3, fecha "lun 28 abr", `space-y-1.5`)
6. Botones Reservación Interna + Generar Cotización en header del calendario de Disponibilidad
7. Vista Mes: pill `bg-blue-50` con total monto programado (`totTent + totDef`, excluye canc)
8. Vista Semana: celdas con resumen por estatus (`{N} tent.`, `{N} def.`, `{N} canc.` + monto)
9. Estatus input: dropdown disabled, fuente `estatuscomercial WHERE visual=true`, `extraIds [2]` en resv-interna
10. Defaults por modo creación: cotización=1 Tentativo, resv-interna=2 Definitivo
11. `estatusid` operacional hardcodeado por modo solo en CREACIÓN: 1 (cot) / 14 (resv-int); en EDIT preserva
12. Cliente selector: limpia email/telefono/empresa/grupo al cambiar (fix bug donde quedaba el del cliente anterior)
13. Búsqueda accent-insensitive en filtros del listado de cotizaciones (NFD + diacritic strip)
14. "Todos los hoteles" oculto en vista Disponibilidad + auto-fallback a session.HotelId al togglear
15. Toggle Disponibilidad/Clásico con paleta azul ligera (más visible sobre fondo blanco)
16. DayDetailSheet: tarjetas compactas (`px-2 py-1.5`, `text-[10px]`), click en card = editar (sin barra inferior)
17. DayDetailSheet: botones Reservación Interna + Generar Cotización en header, footer y estado vacío

**Sync git limpio:**
- Drop de 2 commits wip (4cc55ce + c54c465) que solo tocaban `.planning/`
- Reset --hard a origin/main (incluye los 2 commits de Omar)
- Stash pop sin conflictos (descarté antes mis cambios en auth/session/types porque eran idénticos a los de Omar)
- Push exitoso: `8d9b4cc..15e72b0  main -> main`
</completed_work>

<remaining_work>

**Validación pendiente (post-deploy):**

- 18: Validar Vercel deploy completó y aliases prod sirven `15e72b0`
- 19: Logout + login fresco para que cookie incluya `HotelId` (sesiones viejas no lo tienen → fallback "0" → no preselecciona)
- 20: Validación user de las 17 features en prod

**Arrastres de sesiones previas (no atacados aún):**

- 21: `horaPreMontaje`/`Post`/`horasExtras` en `actualizarCotizacion` modo edición + validaciones solapamiento pre/post con reservaciones
- 22: Cleanup `diagSuffix` en `recalcularPresupuestoPaquete` tras validación de paquetes `tipopersona`/sin-`platobaseid` en prod
- 23: Fix raíz de `eventos.totalmonto` NULL — los pills del Vista Mes muestran `$0` porque `actualizarCotizacion` no recalcula `totalmonto` al cambiar presupuesto. Opciones: (a) recalcular en backend en cada update, (b) vista `vw_oeventos.totalmonto` dinámica (sum de `presupuesto.subtotal*(1-desc/100)+servicio`).
</remaining_work>

<decisions_made>

- **Repurpose del input "Estatus"** → ahora controla `estatuscomercialid` (visible, disabled, default 1/2 por modo). El `estatusid` operacional es invisible/hardcodeado en CREATE y se preserva en EDIT (importante: respeta transiciones como id=3 "Cotizacion Generada" tras PDF).
- **`listaEstatusComercialVisual(extraIds)`** acepta IDs adicionales además del filtro `visual=true`. En resv-interna pasamos `[2]` para que "Definitivo" (visual=false) aparezca y se pueda mostrar como label en el dropdown.
- **Total mes programado = tent + def (no canc)** porque "cotizaciones y eventos programados" excluye cancelados.
- **Descarté mis cambios en auth.ts/session.ts/types/usuarios.ts** antes del sync porque Omar 8d9b4cc ya los tenía idénticos. Evitó conflicto innecesario.
- **Hide "Todos los hoteles" en Disponibilidad** + auto-fallback a session.HotelId al togglear, porque ese calendario requiere hotel específico para listar salones.
- **Click en card = editar** en DayDetailSheet, eliminando la barra inferior con botón Editar (ahorra ~30px/card).
- **Pill toned down**: `bg-blue-50` + `font-semibold` + sin shadow/gradient (iteración del user pidiendo "menos resaltado").
- **Búsqueda accent-insensitive client-side** (NFD + `\p{Diacritic}` strip): no toca SQL.
</decisions_made>

<blockers>
- Ninguno técnico. Solo pendiente validación humana en prod (no bloqueante para iterar localmente).
</blockers>

<context>
Sesión iterativa larga de UX refinements + fix funcional (estatuscomercial). El user iba ajustando preferencias de UI rápido (paddings, colores, tamaños) y yo iteraba. Buena dinámica.

Tres momentos clave:

1. **Sync de git** se hizo CASI al final, después de mucha codificación local. Funcionó bien porque:
   - Los wip commits no tenían código (solo `.planning/` pause-state) → se podían dropear sin pérdida
   - 3 archivos críticos (auth/session/types) eran equivalentes a los de Omar → descarté locales antes de pop stash
   - Resultado: stash pop limpio sin un solo conflicto

2. **El `eventos.totalmonto` NULL** sigue siendo el elefante en el cuarto. Los pills del Vista Mes muestran `$0` y el user lo notó pero aceptó la explicación. Fix queda como tarea 23.

3. **El input de Estatus repurposado** es el cambio más sutil del commit. De afuera parece "el input ahora muestra estatuscomercial", pero internamente: (a) la columna de DB que captura el dropdown cambió de `eventos.estatusid` a `eventos.estatuscomercialid`, (b) `eventos.estatusid` ahora se setea automáticamente por modo (CREATE) y se preserva (EDIT). Si en el futuro esto causa confusión, releer las líneas de `crearCotizacion`/`actualizarCotizacion` y la lógica de `formData.estatusId` en `quotation-form.tsx`.

Memoria: el patrón merge-ours del user con Omar NO se necesitó esta vez (sin conflictos), pero sigue siendo el pattern por defecto cuando los archivos sí choquen.
</context>

<next_action>
Esperar ~2-3 min a que Vercel termine de buildear `15e72b0`. Luego decirle al user:
1. "Logout y login fresco" para regenerar cookie con HotelId
2. Recorrer las 17 features en prod (lista en completed_work) y reportar cualquier issue
3. Si todo OK, atacar tarea 21 (arrastres horaPreMontaje/Post en `actualizarCotizacion`) o tarea 23 (fix `eventos.totalmonto` NULL — la más impactante porque destrabaria los pills $0)
</next_action>
